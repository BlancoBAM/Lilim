[workspace]
members = [
    "crates/lilim-runtime",
    "crates/lilim-inference",
]
exclude = ["lilim_desktop/src-tauri"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "AGPL-3.0"
authors = ["BlancoBAM"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
